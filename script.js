// Small JS for future interactivity
// (Dark mode toggle, animations, etc.)

console.log("prateekKBuilds portfolio loaded 🚀");

document.getElementById("scrollTop").addEventListener("click", () => {
  window.scrollTo({
    top: 0,
    behavior: "smooth"
  });
});

